@model TestHireChannelMVC.Models.RecruiterJobSeeker.JobSeekerModel

<h2>My Applied Jobs</h2>
<table>
    <tr>
        <th>
            Job Title
        </th>
        <th>
            Description
        </th>
        <th>
            Skills
        </th>
    </tr>

@foreach (var item in Model.AppliedJobs) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Job.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Job.Description)
        </td>
        <td>
        @foreach (var skill in item.Skills) {
            <div>
            @Html.DisplayFor(skillItem => skill.Skill)
            </div>
            <div>
            @Html.DisplayFor(skillItem => skill.Year)
            </div>
        }
        </td>
    </tr>
}

</table>
