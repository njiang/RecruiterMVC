
@model TestHireChannelMVC.Models.RecruiterJobSeeker.SearchResultModel

<table>
    <tr>
        <th>
            Job Title
        </th>
        <th>
            Description
        </th>
        <th>
            Skills
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Jobs) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
        </td>
        <td>
            @Ajax.ActionLink("Apply", "ApplyJob", "Jobseeker", new { jobID = item.JobID, userID = Model.User.UserID }, new AjaxOptions() { HttpMethod = "Get" })
        </td>
        @*<td>
        @foreach (var skill in item.Skills) {
            <div>
            @Html.DisplayFor(skillItem => skill.Skill)
            </div>
            <div>
            @Html.DisplayFor(skillItem => skill.Year)
            </div>
        }
        </td>*@
    </tr>
}

</table>

    




